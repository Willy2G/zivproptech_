# Copiez ce fichier en `.env` et adaptez les valeurs.

# Serveur
PORT=4000
# Origine autorisee pour CORS (front Vite)
CORS_ORIGIN=http://localhost:5173

# Base de donnees Postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=ziv_proptech
